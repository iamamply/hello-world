name: 'learn-github-actions'
on: [push]
jobs:
  check-bat-version:
    runs-on: ubuntu-latest
    steps:
      - user: actions/setup-node@v3
        with:
          node-version: 14
      - run: npm install -g bats
      - run: bats -v
